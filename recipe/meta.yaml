{% set name = "schemdraw" %}
{% set version = "0.20" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/schemdraw-{{ version }}.tar.gz
  sha256: deaf028827cd1ab1a62c5d40cb70a0ed32ac1f4243ddd6ca86c616e4ac7a3672

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.7
  run:
    - python >=3.7
    - typing-extensions

test:
  imports:
    - schemdraw
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://schemdraw.readthedocs.io/
  summary: Electrical circuit schematic drawing
  dev_url: https://bitbucket.org/cdelker/schemdraw
  license: MIT
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - dkozel
